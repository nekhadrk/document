step 1 open folder  
step 2 create a readme.md file  
step 3 create a repo in github  
step 4 open git bash in the folder by right clicking on the folder
step5 type the following commands
```sh
git init
git add .
git commit -m "first commit"
 git config --global user.email "nekharamadoss@gmail.com"

 git config --global user.name "nekhadrk"
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/nekhadrk/new.git
git push -u origin main

```
login in git by clicking signin with code 
open chrome and type github.com/login/device
select the github account and paste the code

